version: "3"

services:
  caddy:
    image: caddy
    ports:
      - "80:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - "./mnt/github/:/mnt/github/"

        #renovate:
        #image: renovat
